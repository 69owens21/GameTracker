# GameTracker
Unified cross-platform game library and telemetry tracker built entirely in SQLite. This senior design project tackles complex many-to-many relationships and disparate data sources (PC, Xbox, PS, Switch) using deep normalization, strict schema constraints, and dynamic JSON functions to build a highly optimized, analytics-ready database.
